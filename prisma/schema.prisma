generator client {
  provider        = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model User {
  id            String   @id @default(cuid())
  name          String
  age           Int
  profilePic    String
  dob           DateTime
  bioVideo      String
  profilePicKey String
  bioVideoKey   String
}
